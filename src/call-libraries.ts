import { constants } from '@zerodev/sdk';
import { ENTRYPOINT_ADDRESS_V07, getEntryPointVersion } from 'permissionless';
import type { EntryPointVersion } from 'viem/account-abstraction/types/entryPointVersion';

// Just some imports to trigger the compilation errors
